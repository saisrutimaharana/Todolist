<!DOCTYPE html>
<html>
<head>
    <title>Todo List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .todo-container {
            max-width: 400px;
            margin: 20px auto;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 5px;
        }

        .todo-item {
            display: flex;
            align-items: center;
            padding: 8px;
            border-bottom: 1px solid #ccc;
        }

        .todo-item.completed span {
            text-decoration: line-through;
            color: gray;
        }

        .todo-form {
            display: flex;
            margin-top: 20px;
        }

        .todo-form input[type="text"] {
            flex-grow: 1;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 3px;
            outline: none;
        }

        .todo-form button {
            padding: 8px;
            background-color: #007bff;
            color:  #fff;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }

        .todo-form button:hover {
            background-color: #0056b3;
        }

    </style>
</head>
<body>
    <div class="todo-container">
        <h1>To-Do List</h1>

        <ul>
            <% @todoings.each do |todoing| %>
                <li>
                    <%= task.title %>
                    <%= link_to 'Edit', edit_todoing_path(todoing) %>
                    <%= link_to 'Delete', todoing_path(todoing), method: :delete, data: { confirm: 'Are you sure?' } %>
                </li>
            <% end %>
        </ul>

        <%= link_to 'New Task', new_todoing_path %>


    </div> <
   
</body>
</html>
